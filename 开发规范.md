# Three Against One 开发规范

本文档用于统一项目的代码、玩法、资源和验证流程。目标不是限制创意，而是让每一次新增功能都能沉淀为可复用系统，避免临时逻辑越堆越散。

## 1. 核心开发原则

1. 先做底层规则，再做具体内容。
   - 新单位、新建筑、新科技、新资源效果，优先落在模板、服务或规则模块里。
   - 具体文明、领主、兵种只填写差异化数据，避免在管理器里写死个例。

2. 功能可以小步实现，但结构必须可扩展。
   - 可以先做最小可玩版本。
   - 但入口、数据字段、信号、服务边界要为后续扩展留位置。

3. 管理器负责流程，服务负责规则，模板负责数据。
   - `UnitManager2D`、`BuildingManager2D` 这类管理器负责选择、移动、放置、绘制、信号连接。
   - `TechnologyService`、`AchievementService`、`VictoryService` 这类服务负责可复用规则。
   - `DefaultTemplateLibrary`、各类 `Template` 负责单位、建筑、领主、科技的基础数据。

4. 玩家体验优先于内部完整性，但不能牺牲底层一致性。
   - UI 可以先简化。
   - 玩法规则不要临时绕过模板、资源、AP、回合、战争迷雾等核心系统。

## 2. Godot 与 GDScript 规范

1. 使用 Godot 4.x 写法。
   - `await signal`，不要使用 Godot 3 的 `yield`。
   - `@export`、`@onready`，不要使用旧语法。
   - `instantiate()`，不要使用 `instance()`。

2. 类型要明确。
   - 字典、数组、模板转换处优先显式声明类型。
   - 不在 `Dictionary.get()`、混合类型三元表达式、非类型化数组元素上使用 `:=`。

3. 缩进使用 Tab。
   - GDScript 文件统一使用 Tab 缩进。
   - 避免大段自动格式化导致无关 diff。

4. 脚本职责要清晰。
   - 一个脚本最好只负责一个系统或一类 UI。
   - 如果某个脚本持续膨胀，优先抽成服务、规则类或模板工具。

5. 信号用于模块解耦。
   - 子系统状态变化通过信号通知外部。
   - 不要让远距离节点互相硬引用来驱动复杂流程。

## 3. 模板与数据规范

1. 单位必须走 `UnitTemplate`。
   - 新单位先在模板库中定义 `id`、名称、职业、移动、攻击、生命、视野、射程、标签、招募成本。
   - 单位实例只保存位置、血量、行动状态、状态效果等运行时数据。

2. 建筑必须走 `BuildingData` 或建筑模板。
   - 新建筑应明确类型、建造成本、占地、产出、驻扎规则、升级规则、领主可见性。
   - 需要升级的建筑必须复用建筑升级系统，不要为单个建筑写独立升级逻辑。

3. 领主、科技、成就必须数据化。
   - 领主提供路线、解锁内容和被动修正。
   - 科技定义前置、消耗、效果。
   - 成就定义条件、奖励和所属分支。

4. ID 命名保持稳定。
   - 单位：`unit.<faction>.<name>`，例如 `unit.orc.slinger`。
   - 建筑：`building.<name>`，例如 `building.barracks_lv1`。
   - 科技：`tech.<family>.<name>`。
   - 成就：`<branch>.<topic>.<name>`。

5. 标签用于规则判断。
   - 不要只靠中文名判断逻辑。
   - 常用标签示例：`worker`、`scout`、`military`、`melee`、`ranged`、`orc`、`dragon`、`garrison`。

## 4. 玩法系统开发流程

新增一个玩法功能时，按以下顺序推进：

1. 定义玩法意图。
   - 它解决什么游戏问题？
   - 它属于经济、军事、探索、科技、领主、胜负中的哪一层？

2. 定义底层规则。
   - 消耗什么资源或 AP？
   - 受哪些前置限制？
   - 作用范围、持续时间、结算时机是什么？

3. 放入可复用系统。
   - 能写成模板字段就写成模板字段。
   - 能写成服务规则就写成服务规则。
   - 管理器只负责调用，不承载大量策划判断。

4. 接入 UI。
   - 玩家必须能看懂当前可做什么、为什么不能做、消耗是什么、结果是什么。
   - 调试 UI 可以先做，但不要代替正式交互。

5. 验证一条完整闭环。
   - 例如：建造建筑 → 消耗资源/AP → 产生效果 → 回合结算 → UI 正确显示。

## 5. 数值设计规范

1. 通用建筑使用通用成本。
   - 玩家不等同于单一阵营，不能按“精灵便宜、矮人昂贵”做基础成本。
   - 平衡应从材料种类、产出效率、工人驻扎加成、科技修正、领主建筑中体现。

2. 文明差异主要体现在效率，不体现在基础规则特权。
   - 例如采石场基础产出统一。
   - 矮人工人入驻采石场可获得更高产出，其他工人只有小额加成。

3. 新数值必须说明阶段定位。
   - T1：基础生存与展开。
   - T2：资源链成型。
   - T3：军事与科技分化。
   - T4：高级领主特色与龙族介入。
   - T5：胜负冲刺与终局结算。

4. 资源上限、金币、稀有资源要谨慎。
   - 金币分数权重高，应控制来源和转换效率。
   - 稀有资源用于科技、召唤、特殊建筑，不应在早期大量堆积。

## 6. 战斗与单位规范

1. 单位能力优先字段化。
   - 移动、攻击、生命、视野、射程、标签、状态效果都应进入通用数据。
   - 特殊技能可以是独立动作，但要通过标签或模板 id 开启。

2. 状态效果要有明确生命周期。
   - 例如虚弱持续 3 回合，应在目标玩家回合开始时递减。
   - 状态写入单位运行时 `statuses`，不要散落成多个独立变量。

3. 特殊动作要明确消耗。
   - 是否消耗 AP？
   - 是否消耗攻击机会？
   - 是否影响移动状态？

4. 远程、投掷、召唤、建筑攻击应逐步统一。
   - 先保证玩法可测。
   - 后续再抽象为通用 Action 系统。

## 7. 建筑与资源规范

1. 建筑职责要单一。
   - 农场负责食物持续产出。
   - 仓库负责资源储存上限。
   - 兵营负责军事招募。
   - 铸币厂负责金币链转换。

2. 需要工人参与的建筑必须接入驻扎系统。
   - 生产加成、升级过程、资源转换都优先通过工人驻扎表达。
   - 不为单个建筑写一次性工人占用逻辑。

3. 建筑升级必须复用升级服务。
   - 升级成本、升级等级、工人需求、AP 消耗都应可配置。
   - 升级完成后通过统一接口改变产出、容量或效果。

4. 资源链要闭环。
   - 地图资源点 → 采集/建筑占用 → 资源库存 → 建造/招募/科技/计分。
   - 新资源加入时必须检查 UI、计分、仓库上限和产出来源。

## 8. 科技、成就与任务规范

1. 成就树类似 MC 成就，但服务于科技点来源。
   - 成就是玩家行为记录和科技点奖励来源。
   - 不是单纯展示用的收藏品。

2. 科技树是混合网络，不是文明独立树。
   - 通用科技可以作为领主科技前置。
   - 领主高级科技可以交叉引用通用科技、击杀成就、资源成就。

3. 科技效果必须可查询。
   - 通过 `TechnologyService.get_modifier()` 或类似接口提供。
   - 不要在单位、建筑、资源系统里硬编码某个科技 id。

4. 解锁内容要逐层推进。
   - 成就提供科技点或资源。
   - 科技解锁单位、建筑、动作、修正或召唤。
   - UI 必须显示前置未满足的原因。

## 9. UI 与交互规范

1. UI 要解释玩家当前状态。
   - AP、阶段、资源、选中单位、行动预览、计分规则、科技树入口应清晰可见。

2. 调试信息不要长期占据主界面。
   - 调试面板可以存在，但默认应收起或精简。
   - 玩家看不懂的内部 key 不应直接暴露。

3. 操作反馈必须及时。
   - 可移动格、攻击预览、建造合法性、投掷落点、资源不足都要有明确反馈。

4. 编辑器工具和游戏 UI 分离。
   - 地图编辑画笔、地形预览、撤回等属于开发工具。
   - 不要让编辑器交互污染正式游戏操作。

## 10. 美术资源规范

1. 资源按用途分类。
   - `assets/texture/terrain/`
   - `assets/texture/building/`
   - `assets/texture/character/<faction>/<unit>/`
   - `assets/texture/resource/`
   - 如果现有目录尚未完全整理，新资源也应尽量向这个结构靠拢。

2. 文件命名表达用途。
   - 角色动画：`Idle`、`Walk`、`Attack`、`Hurt`、`Death`。
   - 地形变体：`water_middle`、`clif_corner_up`、`clif_u_left`。

3. Sprite 序列要记录帧数和帧尺寸。
   - 例如血斧兵使用 100x100 帧。
   - 新动画接入时要明确帧数、播放时长、镜像规则。

4. 不要把地形自动拼接和手动画笔混在同一个概念里。
   - 自动岸边规则是一套系统。
   - 手动画笔贴图是另一套系统。
   - 两者可以共享贴图，但不能混淆来源。

## 11. 验证流程

每次功能修改后至少执行：

```powershell
& 'C:\tools\godot\Godot_v4.6.2-stable_win64_console.exe' --path . --headless --check-only --quit
```

通过标准：

1. 没有 GDScript 解析错误。
2. 没有新增运行时报错。
3. 允许已有资源放置警告和退出时资源占用提示，但不能把新的逻辑错误混在里面忽略。

关键玩法还需要手动验证：

1. 开局是否正常。
2. 回合切换是否正常。
3. AP 是否正确消耗。
4. 资源是否正确增减。
5. 战争迷雾是否符合当前玩家视角。
6. UI 是否能解释当前状态。

## 12. Git 与改动规范

1. 不回滚他人改动。
   - 工作区可能已有未提交内容。
   - 修改前先看相关文件上下文，避免覆盖用户或其他工具产生的改动。

2. 改动要集中。
   - 一个功能尽量只改相关模块。
   - 不把格式化、重命名、重构和玩法改动混在同一次无说明修改里。

3. 提交说明要描述玩法结果。
   - 推荐格式：`feat: 添加兽人投石兵投掷动作`
   - 或：`fix: 修复战争迷雾下敌方单位可见`

4. 临时测试内容要标明。
   - 如果为了测试在开局放置额外单位，应在后续正式平衡前移除或转成配置项。

## 13. 新功能检查清单

新增单位：

- [ ] 是否有 `UnitTemplate`
- [ ] 是否有稳定 `template_id`
- [ ] 是否配置招募成本、AP、回合数
- [ ] 是否配置标签
- [ ] 是否接入招募建筑
- [ ] 是否显示正确属性
- [ ] 是否通过战争迷雾与回合规则验证

新增建筑：

- [ ] 是否有建筑数据或模板
- [ ] 是否定义成本、占地、地形兼容
- [ ] 是否接入建造 UI
- [ ] 是否接入资源消耗和 AP 消耗
- [ ] 是否需要驻扎或升级
- [ ] 是否接入计分规则

新增科技：

- [ ] 是否有科技 id
- [ ] 是否有前置
- [ ] 是否有科技点成本
- [ ] 是否有成就或任务来源
- [ ] 是否通过服务暴露效果
- [ ] UI 是否能显示解锁状态

新增资源链：

- [ ] 是否有地图来源或建筑来源
- [ ] 是否进入库存
- [ ] 是否受仓库上限影响
- [ ] 是否有用途
- [ ] 是否进入计分规则

新增战斗动作：

- [ ] 是否明确 AP 消耗
- [ ] 是否明确是否消耗攻击机会
- [ ] 是否处理合法目标
- [ ] 是否处理死亡、奖励和成就
- [ ] 是否有必要的动画或反馈

